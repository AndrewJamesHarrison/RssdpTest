<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Rssdptest"
             x:Class="Rssdptest.MainPage">
    <StackLayout>
        <Button Text="{Binding Test1}" Command="{Binding TestCommand}"/>
        <Label Text="{Binding Test2, Mode=TwoWay}"/>
        <Label Text="{Binding DeviceCount, StringFormat='{0}'}"/>
        <ListView ItemsSource="{Binding DeviceNames}"/>
    </StackLayout>

</ContentPage>